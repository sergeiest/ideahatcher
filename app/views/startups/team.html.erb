<%= render 'users/usernav' %>

<div class="container-ideas">
  <div class="container">
    <div class="left-company">
      <%= render 'info' %>
      <%= render :partial => 'companyteams/employeedetailed', :collection => @startup_teams %>
    </div>

    <div class="right-company">
      <%= render 'actions' %>
    </div>
  </div>
</div>