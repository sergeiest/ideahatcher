.item{:class => "#{'active' if picture_counter == 0}"}
	= image_tag picture.avatar, :class => 'proto_pic'
	.carousel-caption
		%h5.picture_title #{picture.title}
		%p.picture_desc #{picture.description}